# Set these in your process environment or container. The server does not read .env automatically.
HOST=127.0.0.1
PORT=3001
DATABASE_PATH=.data/last-roll.db
# Set the exact HTTPS origin when hosting behind a TLS reverse proxy.
# PUBLIC_ORIGIN=https://your-domain.example
NODE_ENV=production
